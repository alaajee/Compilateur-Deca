// Sorite 16

{
    int x = 10;
    int y = 15;
    int z = 20;
    int result = 0;

    while (z > 10) {
      if ((z % 6 != 0 || z % 5 == 0 || z % 4 == 0) && z % 13 != 0 ) {
                   result = result + 2;
          }
               z = z - 1;
           }
    // Affichage final
    // Sortie 16
    println("Le résultat final est : ", result);
}


