// Sorite 10

{
    int x = 10;
    int y = 15;
    int z = 20;
    int result = 0;


   while (z > 10) {
       if (z % 2 == 0 || z % 3 == 0 || z % 5 == 0
                   || z % 7 == 0 || z % 11 == 0 || z % 13 == 0
                   || z % 17 == 0 || z % 19 == 0 || z % 23 == 0) {
                   result = result + 1;
            }
               z = z - 1;
           }

    // Affichage final
    println("Le résultat final est : ", result); // 10
}