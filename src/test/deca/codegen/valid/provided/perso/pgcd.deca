// Sortie Le PGCD des deux nombres est : 14

{
    int a = 56; // Modifiez cette valeur pour tester
    int b = 98; // Modifiez cette valeur pour tester
    int temp;

    while (b != 0) {
        temp = b;
        b = a % b;
        a = temp;
    }

    println("Le PGCD des deux nombres est : " , a);
}
