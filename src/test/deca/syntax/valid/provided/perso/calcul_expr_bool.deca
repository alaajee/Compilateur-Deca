{
    int a = 10;
    int b = 20;
    int c = 30;

    boolean result = (a + b > c) && ((c - b) < a * 2) || (a % 2 == 0);
    if (!result)
    {
        println("Résultat de l'expression booléenne : faux" );
    }
    else{
        println("Résultat de l'expression booléenne : vrai");

    }
}
