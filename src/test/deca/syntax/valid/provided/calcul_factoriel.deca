class Factorial {
    int compute(int n) {
        if (n <= 1) {
            return 1;
        }
        return n * compute(n - 1);
    }
}

{
    Factorial f = new Factorial();
    println(f.compute(5));
}
